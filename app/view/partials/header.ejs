<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SIMKOS - Pro Dashboard</title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <style>
        :root {
            --sidebar-bg: #1e1b4b;     
            --sidebar-text: #a5b4fc;   
            --active-item: #4338ca;    
            --bg-body: #f1f5f9;        
            --primary-color: #4f46e5;
            --sidebar-width: 260px;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: var(--bg-body);
            color: #334155;
            overflow-x: hidden;
        }

        #wrapper {
            display: flex;
            width: 100%;
            min-height: 100vh;
        }

        #sidebar-wrapper {
            width: var(--sidebar-width);
            min-height: 100vh;
            background-color: var(--sidebar-bg);
            color: white;
            flex-shrink: 0;
            display: flex;
            flex-direction: column;
            position: fixed;
            z-index: 1000;
            box-shadow: 4px 0 10px rgba(0,0,0,0.1);
        }

        .sidebar-brand {
            padding: 1.5rem;
            font-size: 1.35rem;
            font-weight: 800;
            color: white;
            display: flex;
            align-items: center;
            gap: 12px;
            border-bottom: 1px solid rgba(255,255,255,0.1);
            background-color: rgba(0,0,0,0.1);
        }

        .list-group-item {
            background: transparent;
            border: none;
            padding: 14px 24px;
            color: var(--sidebar-text);
            font-weight: 500;
            font-size: 0.95rem;
            margin-bottom: 4px;
            margin-right: 15px;
            border-radius: 0 50px 50px 0; 
            transition: all 0.3s;
        }

        .list-group-item:hover {
            background-color: rgba(255,255,255,0.1);
            color: white;
            padding-left: 28px;
        }

        .list-group-item.active {
            background-color: var(--active-item); 
            color: white;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.2);
            font-weight: 600;
        }

        .list-group-item i {
            margin-right: 12px;
            font-size: 1.2rem;
            vertical-align: middle;
        }

        #page-content-wrapper {
            margin-left: var(--sidebar-width);
            flex-grow: 1;
            width: 100%;
            display: flex;
            flex-direction: column;
        }

        .top-navbar {
            background: linear-gradient(to right, #1e1b4b, #4f46e5); 
            color: white;
            padding: 15px 30px;
            position: sticky;
            top: 0;
            z-index: 999;
            box-shadow: 0 4px 20px rgba(79, 70, 229, 0.2);
            border-bottom: none;
        }

        .date-badge {
            background: rgba(255, 255, 255, 0.15);
            backdrop-filter: blur(5px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            color: white;
            padding: 8px 20px;
            border-radius: 50px;
            font-size: 0.85rem;
            font-weight: 500;
        }

        .card {
            border: none;
            border-radius: 16px;
            background: white;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05);
            transition: transform 0.2s;
        }
        .card:hover { transform: translateY(-3px); }

        .badge-soft-success { background-color: #dcfce7; color: #166534; }
        .badge-soft-danger  { background-color: #fee2e2; color: #991b1b; }
        .badge-soft-warning { background-color: #fef3c7; color: #92400e; }
        .badge-soft-primary { background-color: #e0e7ff; color: #3730a3; }

        @media (max-width: 768px) {
            #wrapper { flex-direction: column; }
            #sidebar-wrapper {
                width: 100%;
                height: auto;
                min-height: auto;
                position: relative;
                background-color: var(--sidebar-bg);
            }
            #page-content-wrapper { margin-left: 0; }
            .list-group {
                flex-direction: row;
                overflow-x: auto;
                padding: 10px;
                white-space: nowrap;
            }
            .list-group-item {
                border-radius: 8px;
                margin-right: 5px;
                padding: 10px 15px;
            }
            .container-fluid { padding: 15px !important; }
            .navbar-center-title { display: none !important; }
        }
    </style>
</head>
<body>

<div id="wrapper">
    <div id="sidebar-wrapper">
        <div class="sidebar-brand">
            <i class="bi bi-buildings-fill text-warning"></i> &nbsp;SiMKos App
        </div>
        
        <div class="list-group list-group-flush mt-3 pe-0">
            <a href="/" class="list-group-item list-group-item-action <%= (typeof page !== 'undefined' && page === 'Dashboard') ? 'active' : '' %>">
                <i class="bi bi-grid-fill"></i> Dashboard
            </a>

            <a href="/kamar" class="list-group-item list-group-item-action <%= (typeof page !== 'undefined' && page === 'Manajemen Kamar') ? 'active' : '' %>">
                <i class="bi bi-door-open-fill"></i> Data Kamar
            </a>

            <a href="/penyewa" class="list-group-item list-group-item-action <%= (typeof page !== 'undefined' && page === 'Manajemen Penghuni') ? 'active' : '' %>">
                <i class="bi bi-people-fill"></i> Data Penyewa
            </a>

            <a href="/penyewaan" class="list-group-item list-group-item-action <%= (typeof page !== 'undefined' && page === 'Manajemen Transaksi') ? 'active' : '' %>">
                <i class="bi bi-receipt"></i> Transaksi
            </a>
        </div>
        
        <div class="mt-auto p-4 border-top border-secondary">
            <div class="d-flex align-items-center gap-3">
                <div class="rounded-circle bg-primary text-white d-flex align-items-center justify-content-center shadow-sm" 
                    style="width:42px; height:42px; background: linear-gradient(45deg, #4f46e5, #3b82f6);">
                    <i class="bi bi-person-fill fs-5"></i>
                </div>

                <div style="line-height:1.2">
                    <div class="fw-bold text-white" style="font-size: 0.9rem;">Admin Kos</div>
                    <small class="text-success fw-bold" style="font-size:0.75rem;">● Online</small>
                </div>
            </div>
        </div>
    </div>

    <div id="page-content-wrapper">
        <nav class="navbar navbar-expand-lg top-navbar">
            <div class="container-fluid d-flex justify-content-between align-items-center position-relative">
                
                <div class="d-flex align-items-center">
                    <h4 class="mb-0 fw-bold text-white">
                        <% if (typeof page !== 'undefined') { %>
                            <%= page %>
                        <% } else { %>
                            Dashboard
                        <% } %>
                    </h4>
                </div>

                <div class="navbar-center-title position-absolute start-50 translate-middle-x">
                    <h5 class="mb-0 text-white fw-bold text-uppercase" style="letter-spacing: 1px; opacity: 0.9;">
                        Sistem Manajemen Kos
                    </h5>
                </div>

                <div class="d-none d-md-block date-badge shadow-sm">
                    <i class="bi bi-calendar2-week me-2"></i>
                    <%= new Date().toLocaleDateString('id-ID', { weekday: 'long', day:'numeric', month:'long', year:'numeric' }) %>
                </div>

            </div>
        </nav>
        
        <div class="container-fluid px-4 py-4">